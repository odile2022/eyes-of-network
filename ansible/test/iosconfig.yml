# ansible-playbook iosconfig.yml -i hosts
- hosts: ROUTERS
  gather_facts: false
  connection: local

  tasks:
    - name: change Hostname R
      ios_config:
        lines:
          - hostname  R-changed



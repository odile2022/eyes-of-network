# ansible-playbook iosconfig.yml -i hosts
- hosts: all
  gather_facts: false
  connection: local

  tasks:
    - name: "change Hostname to {{ v_hostname }}"
      ios_config:
        lines:
          - hostname  "{{ v_hostname }}"
